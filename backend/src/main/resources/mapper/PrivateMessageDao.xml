<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gcs.dao.PrivateMessageDao">
    
    <resultMap id="conversationResultMap" type="com.gcs.vo.ConversationVO">
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="avatar" property="avatar"/>
        <result column="last_message" property="lastMessage"/>
        <result column="last_time" property="lastTime"/>
        <result column="unread_count" property="unreadCount"/>
    </resultMap>
    
    <!-- 查询两个用户之间的聊天记录 -->
    <select id="selectChatHistory" resultType="com.gcs.entity.PrivateMessage">
        SELECT * FROM private_message
        WHERE (from_user_id = #{userId1} AND to_user_id = #{userId2})
           OR (from_user_id = #{userId2} AND to_user_id = #{userId1})
        ORDER BY create_time DESC
    </select>
    
    <!-- 查询会话列表 -->
    <select id="selectConversations" resultMap="conversationResultMap">
        WITH ranked_messages AS (
            SELECT
                pm.*,
                CASE
                    WHEN pm.from_user_id = #{userId} THEN pm.to_user_id
                    ELSE pm.from_user_id
                END AS opponent_id,
                ROW_NUMBER() OVER (
                    PARTITION BY
                        CASE
                            WHEN pm.from_user_id = #{userId} THEN pm.to_user_id
                            ELSE pm.from_user_id
                        END
                    ORDER BY pm.create_time DESC
                ) AS rn
            FROM private_message pm
            WHERE pm.from_user_id = #{userId} OR pm.to_user_id = #{userId}
        ),
        unread_counts AS (
            SELECT
                opponent_id,
                SUM(CASE WHEN is_unread = 1 THEN 1 ELSE 0 END) AS unread_count
            FROM (
                SELECT
                    CASE
                        WHEN pm.from_user_id = #{userId} THEN pm.to_user_id
                        ELSE pm.from_user_id
                    END AS opponent_id,
                    CASE
                        WHEN pm.to_user_id = #{userId} AND pm.status = 0 THEN 1
                        ELSE 0
                    END AS is_unread
                FROM private_message pm
                WHERE pm.from_user_id = #{userId} OR pm.to_user_id = #{userId}
            ) AS msg_with_status
            GROUP BY opponent_id
        )
        SELECT
            rm.opponent_id AS user_id,
            u.nickname AS username,
            u.avatar AS avatar,
            rm.content AS last_message,
            rm.create_time AS last_time,
            COALESCE(uc.unread_count, 0) AS unread_count
        FROM ranked_messages rm
        INNER JOIN users u ON rm.opponent_id = u.id
        LEFT JOIN unread_counts uc ON rm.opponent_id = uc.opponent_id
        WHERE rm.rn = 1
        ORDER BY rm.create_time DESC
    </select>
    
    <!-- 统计未读消息数 -->
    <select id="countUnreadMessages" resultType="java.lang.Integer">
        SELECT COUNT(*) 
        FROM private_message
        WHERE to_user_id = #{currentUserId}
          AND from_user_id = #{otherUserId}
          AND status = 0
    </select>
    
    <!-- 标记消息为已读 -->
    <update id="markAsRead">
        UPDATE private_message
        SET status = 1
        WHERE to_user_id = #{currentUserId}
          AND from_user_id = #{fromUserId}
          AND status = 0
    </update>
    
</mapper>
