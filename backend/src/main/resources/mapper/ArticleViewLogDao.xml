<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gcs.dao.ArticleViewLogDao">

    <insert id="insertIgnore">
        INSERT INTO article_view_log (article_id, viewer_key, view_date, create_time)
        VALUES (#{articleId}, #{viewerKey}, CURRENT_DATE, NOW())
        ON CONFLICT (article_id, viewer_key, view_date) DO NOTHING
    </insert>

</mapper>
